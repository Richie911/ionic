import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.devop',
  appName: 'devop',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
